# 2026-07-24

## 17:13 KST - Common post-publication release closure

### Summary

- Kept Comins Contract v1.2 and added `candidate`, `staged`, `published`, and `closed` release states to the canonical release policy.
- Defined a public release as incomplete until the exact registry version and dist-tag, artifact integrity and provenance, public consumer smoke, source merge, release evidence, and local/remote default-branch state are verified.
- Added the recurring post-publication checklist and kept release branch or worktree deletion behind separate maintainer approval.
- Updated the canonical module guidance and synchronized only its managed block to Data Table, Grid Layout, and Sortable. Module-owned guidance and managed project configuration content were preserved.

### Changed Files

- `RELEASE_POLICY.md`
- `MODULE_CHECKLIST.md`
- `CHANGELOG.md`
- `templates/module/AGENTS.template.md`
- `test/policy-contract.test.mjs`
- `reports/2026-07-24.md`
- Each current module's root `AGENTS.md`

### Validation

- The focused policy contract test failed before the release states and closure policy existed, then passed after implementation.
- Governance `node --test`: 61/61 passed.
- Data Table guidance/security test: 7/7 passed.
- Grid Layout guidance/security test: 6/6 passed.
- Sortable guidance/security test: 6/6 passed.
- Governance and all three module project configurations parsed with `codex --strict-config --version` on Codex CLI 0.144.4.
- Module diffs contained only the two new managed reporting rules; `.codex/config.toml` and module-owned guidance had no diff.
- `git diff --check` passed for all three module changes.

### Failure Classification

- The first Data Table synchronization was rolled back when the sandbox denied a no-op write open on the managed `.codex/config.toml`. Target root, markers, file type, and content were valid. The same deterministic update passed with approved filesystem access, so this was classified as an execution-environment permission failure.
- A focused test initially rejected the valid `**Candidate:**` Markdown label because its regular expression omitted the colon. The test contract was corrected to require the intended label.

### Residual Risks

- The common policy and module adoptions are local changes only. No commit, push, pull request, merge, publish, tag, release, branch deletion, or worktree deletion was performed.
- Data Table 0.1.4 and Grid Layout 0.1.5 remain published but not yet closed under the new policy until their public consumer checks, release evidence, and default-branch reconciliation are completed.
- Product, build, and browser gates were not run because this change affects guidance and release policy only.
