# 2026-07-22 Governance Work Report

> 각 섹션은 해당 시점의 작업 기록이며, 현재 정책은 저장소 루트 정책 문서를 따른다.

## 작업 일시

- 2026-07-22 00:38 KST

## 요약

- `codex/privacy-baseline-simple`을 로컬 `main`에 fast-forward 병합했다.
- Comins Contract v1.2, 공통 민감정보 표준, Gitleaks 및 raw Git identity gate를 병합된 `main`에서 재검증했다.
- 저장소 로컬 Git 설정에 `core.hooksPath=.githooks`를 적용하고 실제 pre-commit 훅을 검증했다.

## 검증

- `node --test test/*.test.mjs`: 18/18 통과
- `origin/main..main` raw identity 검사: 통과
- `origin/main..main` Gitleaks 검사: 통과
- `git diff --check origin/main..main`: 통과
- tracked pre-commit 훅 실행: 통과
- worktree 상태: clean

## 원격 감사

- Secret Protection과 Push Protection은 활성 상태다.
- Private Vulnerability Reporting은 비활성 상태다.
- `main` ruleset과 classic branch protection은 구성되지 않았다.
- 새 보안 workflow는 로컬 `main`에만 있으므로 원격 required check로 아직 사용할 수 없다.

## 잔여 조치

- 원격 `main` 반영 후 보안 workflow를 실행하고 Governance `main` ruleset을 생성해야 한다.
- Private Vulnerability Reporting 활성화는 별도 GitHub 설정 변경 승인이 필요하다.
- 원격 push와 GitHub 설정 변경은 이번 로컬 병합에서 수행하지 않았다.

## GitHub Service Committer Compatibility — 01:32 KST

### 요약

- Contract v1.2가 허용하는 GitHub 서비스 신원 중 Dependabot 커밋에 사용되는 GitHub committer 신원이 checker에서 누락된 문제를 수정했다.
- 실제 Dependabot 형태의 author/committer 조합을 만드는 회귀 테스트를 먼저 추가해 실패를 확인한 뒤 최소 허용 항목을 추가했다.

### 변경 파일

- `scripts/check-public-identities.mjs`
- `test/public-identities.test.mjs`

### 검증

- 회귀 테스트 수정 전 실패 확인
- `node --test test/public-identities.test.mjs`: 13/13 통과
- `node --test test/*.test.mjs`: 19/19 통과

### 잔여 리스크

- checker 동작 범위의 잔여 리스크는 확인되지 않았다. npm 계정 및 legacy provider metadata 감사는 별도 운영 항목이다.

## Comins-reference Skill — 06:35 KST

### 요약

- 신규 독립 Comins 저장소의 공통 `AGENTS.md` 초기화와 기존 저장소의 공통 지침 갱신을 담당하는 `comins-reference` 스킬을 추가했다.
- Governance 템플릿에 Contract v1.2 관리 블록 경계를 추가하고, 모듈별 지침은 경계 밖에서 보존하도록 했다.
- 기존 무표식 `AGENTS.md`는 자동 덮어쓰지 않고 검토된 1회 마이그레이션을 요구한다.
- Governance 원본을 사용자 스킬 경로에 심볼릭 링크하여 Data Table, Grid Layout, Sortable에서 읽을 수 있도록 했다.

### 변경 파일

- `.agents/skills/comins-reference/SKILL.md`
- `.agents/skills/comins-reference/agents/openai.yaml`
- `.agents/skills/comins-reference/scripts/sync-guidance.mjs`
- `templates/module/AGENTS.md`
- `README.md`
- `CHANGELOG.md`
- `test/comins-reference-skill.test.mjs`
- `test/comins-reference-sync.test.mjs`
- `test/policy-contract.test.mjs`
- `docs/superpowers/specs/2026-07-22-comins-reference-skill-design.md`
- `docs/superpowers/plans/2026-07-22-comins-reference-skill.md`

### 검증

- 무스킬 초기화·업데이트 기준 시나리오를 실행해 수동 정책 재해석과 공통 문구 drift 가능성을 확인했다.
- 관리 경계 테스트와 동기화 helper 테스트의 RED 실패를 확인한 뒤 구현했다.
- `node --test test/comins-reference-sync.test.mjs`: 5/5 통과
- `node --test test/comins-reference-skill.test.mjs`: 4/4 통과
- `node --test test/policy-contract.test.mjs`: 8/8 통과
- 공식 `quick_validate.py`: 통과
- 스킬 적용 초기화·legacy migration 시나리오: 공통/모듈 경계, 값 비노출 실패, 외부 작업 승인 경계 준수 확인
- 사용자 스킬 링크를 통한 기존 세 모듈 호출: 예상된 legacy fail-closed, `AGENTS.md` 무변경 확인

### 잔여 리스크

- 현재 세 모듈의 기존 `AGENTS.md`는 아직 관리 marker가 없으므로 각 저장소에서 스킬을 처음 실행할 때 검토된 1회 마이그레이션이 필요하다.
- 작성 시점 기준 스킬 변경은 원격 `main`에 미반영이며 별도 PR·merge가 필요하다.

## Comins Guidance Renewal — 17:03 KST

### 요약

- 공통 지침을 작업 위험도에 따라 조사·설계·계획·회귀 테스트·검증 깊이를 선택하도록 갱신하고, 모든 작업에 하나의 절차를 강제하던 흐름을 제거했다.
- Sol `xhigh` 기본값을 Markdown 문구에서 managed `.codex/config.toml`로 이동했다.
- `comins-reference`가 `AGENTS.md`와 project config를 전체 사전 검증 후 함께 동기화하도록 확장했다.
- 반복 감사를 위한 `comins-updatemd` 스킬, 값 비노출 instruction inventory, 선택형 집계 telemetry helper를 추가하고 사용자 공통 스킬 경로에 연결했다.
- marker는 독립된 한 줄만 허용하고 managed surface symlink를 거부하도록 동기화 helper를 보강했다. 여러 telemetry 입력 중 하나라도 schema를 인식하지 못하면 전체를 `unavailable`로 분류한다.
- 개발 흐름 infographic도 회귀 테스트·설계·플랜·broad gate가 조건에 해당할 때만 선택되도록 문구를 보정했다.
- Data Table, Grid Layout, Sortable을 각 독립 저장소 변경으로 적용했으며 제품 코드와 Contract v1.2는 변경하지 않았다.

### 주요 변경 파일

- `AGENTS.md`, `.codex/config.toml`
- `templates/module/AGENTS.template.md`, `templates/module/.codex/config.toml`
- `.agents/skills/comins-reference/`
- `.agents/skills/comins-updatemd/`
- `test/comins-reference-*.test.mjs`, `test/policy-contract.test.mjs`
- `test/comins-updatemd-skill.test.mjs`, `test/inventory-instructions.test.mjs`, `test/summarize-codex-telemetry.test.mjs`
- `README.md`, `CHANGELOG.md`
- `docs/superpowers/specs/2026-07-22-comins-guidance-renewal-design.md`
- `docs/superpowers/specs/2026-07-22-comins-development-flow-renewal.png`
- `docs/superpowers/plans/2026-07-22-comins-guidance-renewal.md`

### 검증

- RED 기준: 신규 계약 focused suite에서 예상된 20건 실패를 확인했다.
- focused Governance suite: 33/33 통과.
- post-review focused tests: `comins-reference` sync 13/13, inventory 6/6, telemetry 6/6 통과.
- 전체 Governance suite: 56/56 통과.
- 공식 `quick_validate.py`: `comins-reference`, `comins-updatemd` 모두 통과. 시스템 Python의 PyYAML 부재는 `/tmp` 격리 `uv` 환경으로 해결했으며 전역 환경은 변경하지 않았다.
- 네 저장소 Codex prompt discovery/config parse: 값 비노출 JSON parse 통과. `codex --strict-config --version`: 모두 통과.
- `comins-updatemd` forward test: local-only 감사에서 불필요한 plan, TDD, 제품 gate를 실행하지 않고 stale tracking ref를 비차단 증거로 정확히 분류했다.
- 갱신 후 instruction inventory: 네 저장소에서 model prose, blind pre-read, mandatory process-chain 발견 0건.
- repository-local automatic guidance bytes와 최대 발견 Codex chain은 Governance `2571/2571`, Data Table `4990/4990`, Grid Layout `4358/5896`, Sortable `3409/3409`였다. 앞 숫자는 root, 뒤 숫자는 최대 발견 chain이며 품질 임계값으로 사용하지 않았다.

### 잔여 리스크

- rollout JSONL을 지정하지 않아 시간·토큰 telemetry는 0이 아니라 미측정이다. 통계적 A/B 평가는 실행하지 않았다.
- 17:03 KST 구현 checkpoint에서는 원격 서버 상태를 fetch하지 않았으며, 로컬 tracking ref 기준 Data Table과 Grid Layout은 뒤처져 있었다. 로컬 지침 변경의 비차단 상태로만 분류했다.
- Data Table과 Grid Layout의 consumer archive와 publish archive 일치 여부는 별도 release 구현 과제다.
- 17:03 KST 구현 checkpoint에서는 commit, push, PR, publish, tag, GitHub Release, provider 변경을 수행하지 않았다. 후속 승인 범위는 저장소별 로컬 커밋까지이며 원격 쓰기는 포함하지 않는다.

## Activated Skill Chain Alignment — 18:24 KST

### 요약

- 갱신 후 활성 Skill 체인을 다시 감사하여 외부 Superpowers 플러그인의 `using-superpowers`, `brainstorming`, `test-driven-development`, `writing-plans`가 위험도 기반 Comins 흐름보다 넓게 활성화되는 충돌을 확인했다.
- maintainer 승인에 따라 플러그인은 유지하고, 개인 Codex 설정의 공식 per-Skill 활성화 설정으로 위 네 Skill만 비활성화했다. 외부 플러그인 캐시와 bundled source는 수정하지 않았다.
- `comins-updatemd`가 외부 소유 Skill 충돌을 발견하면 캐시를 수정하지 않고 소유권·충돌·잔여 활성화를 보고하며, 개인 설정 범위가 명시된 경우에만 공식 enable/disable 설정을 사용하도록 보강했다.

### 변경 파일

- `.agents/skills/comins-updatemd/SKILL.md`
- `test/comins-updatemd-skill.test.mjs`
- `reports/2026-07-22.md`
- 개인 Codex 설정은 저장소 외부에서 변경했으며 추적하거나 경로를 복사하지 않았다.

### 검증

- 신규 Skill 계약 RED: 기존 Skill에서 4/5 통과, 외부 활성화 설정 계약 1건 실패 확인.
- focused Skill 테스트: 5/5 통과.
- 전체 Governance 테스트: 57/57 통과.
- 공식 `quick_validate.py`: `/tmp` 격리 환경에서 통과.
- Codex strict config parse: 통과.
- 네 저장소 instruction inventory: mandatory process chain 및 broad verification 후보 0건.
- `git diff --check`: 통과.

### 잔여 리스크

- 현재 세션의 초기 Skill 목록은 다시 로드되지 않으므로 Codex 재시작 또는 새 세션 이후 활성화 상태를 최종 확인해야 한다.
- per-Skill 비활성화는 설치된 플러그인 Skill 경로를 기준으로 하므로 플러그인 버전 변경 후 `comins-updatemd`로 활성 체인을 다시 감사해야 한다.
- rollout JSONL 기반 시간·토큰 telemetry와 통계적 A/B 평가는 여전히 미측정이다.

## Pre-merge Telemetry Availability Fix — 18:59 KST

### 요약

- 병합 전 독립 리뷰에서 `summarize-codex-telemetry.mjs`가 구조 레코드만 인식한 입력의 미관측 duration·token metric을 실제 `0`으로 출력하는 계약 위반을 확인했다.
- 구조 인식과 입력별·metric별 관측 상태를 분리했다. 전체 입력에서 확인되지 않은 metric은 `status: partial`, `null`, 상수 diagnostic으로 반환하고 human output에서는 `unmeasured`로 표시한다.
- 단일 입력 누락, 일부 token field 누락, 복수 입력 중 한 입력의 metric 누락을 회귀 테스트로 고정했다.

### 변경 파일

- `.agents/skills/comins-updatemd/scripts/summarize-codex-telemetry.mjs`
- `test/summarize-codex-telemetry.test.mjs`
- `reports/2026-07-22.md`

### 검증

- RED: focused telemetry 8건 중 신규 계약 2건 실패를 확인했다.
- GREEN: focused telemetry 9/9 통과.
- 전체 Governance 테스트 60/60 통과.
- 공식 `quick_validate.py`: 기존 `/tmp` 격리 cache를 사용한 offline 실행에서 `comins-reference`, `comins-updatemd` 모두 통과.
- Codex strict config parse와 네 저장소 `git diff --check` 통과.
- 동일 독립 리뷰어 재검토에서 Critical·Important 0건, merge 가능 판정을 확인했고 Minor 다중 입력 권고도 회귀 테스트로 반영했다.

### 잔여 리스크

- 실제 rollout JSONL을 지정한 시간·토큰 측정과 통계적 A/B 평가는 이번 수정 범위에 포함하지 않았다.
